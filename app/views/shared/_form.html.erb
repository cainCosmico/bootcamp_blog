  <main class="relative container mx-auto bg-transparent px-4 py-8 h-screen">
    <article class="max-w-prose mx-auto pb-8">
      <h1 class="text-2xl font-bold dark:text-gray-100"> <%= yield :title %> </h1>
      <div class="flex items-center justify-center">
        <div class="container max-w-screen-lg mx-auto">
          <div class="bg-white dark:bg-gray-600 rounded shadow-lg p-4 px-4 md:p-8 mb-6">
            <div class="grid gap-4 gap-y-2 text-sm grid-cols-1 ">
              
              <%# <div class="lg:col-span-2"> %>
                <%# <div class="grid gap-4 gap-y-2 text-sm grid-cols-1 md:grid-cols-5"> %>

<div class="lg:col-span-2">

                    <%= yield :content %>                 

</div>

                <%# </div> %>
              <%# </div> %>

            </div>
          </div>
        </div>
      </div>
    </article>
  </main>